
  const doorToDoor = (volunteers, neighbourhoods) => {
    // Code here!
    let numVolunteers = volunteers.length;
    let numNeighbourhoods = neighbourhoods.length;
    let split = numNeighbourhoods/numVolunteers;
    return split
    // Remember to return a value
    // Want to see what's going on inside your function? You can use console.log() to debug and print variables to the console below.

  }
